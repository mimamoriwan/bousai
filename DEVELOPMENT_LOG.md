# 📖 みまもりWAN 開発手帳（DEVELOPMENT_LOG）

本ドキュメントは、プロジェクトの方向性、進行中の開発タスク、各種課題を随時整理・管理するためのロードマップおよび課題管理表です。

## 1. 📝 プロジェクトの基本方針と背景

*   **目的**: 愛犬と歩いて作る地域の安全マップ。知っていれば防げる危険を共有し、地域の安全とペットに優しいまちづくりを目指す。
*   **重要決定事項（アーキテクチャ・運用）**:
    *   LINE公式の無料メッセージ枠（月200通）の制限を回避するため、アプリのアップデート等のお知らせはLINEのメッセージ個別配信に依存しない方針とする。
    *   **解決策**: 「アプリ内の『ガイド画面』へのアップデート履歴の直接記載」と「LINE VOOMへのリンク誘導（お知らせ・確認ボタン）」の合わせ技を採用して運用する。

## 2. 🚀 現在進行中のタスク（In Progress）

（現在特になし）

## 3. 📋 次にやるべきタスク（To Do / Backlog）

### 📱 【新規タスク】LINE公式アカウントのUX改善・自動応答の拡充
*   **「使い方・ヘルプ」の整備**: リッチメニューで「使い方・ヘルプ」をタップした際に出る未対応メッセージを解消し、アプリ内のガイド画面（Guide.jsx）へスムーズに誘導する応答メッセージを設計・実装する。
*   **特定キーワードへの自動応答**: ユーザーがよく入力するであろうワードに対する動作案内や、適切な返信メッセージのシナリオ作成と設定の準備。

### 🐾 【新機能】お散歩予報（LINE応答版）実装ロードマップ
ユーザーがLINEの「今から行ける？」ボタンを押すと、マイページに登録した住所を元に、ピンポイントの雨雲・天気情報を即座に返信する機能。LINEの「応答メッセージ」を活用することで、プッシュ通知の無料枠（月200通）を消費せずに無制限・無料で運用する。
*   **フェーズ1**: 準備とAPIキーの取得（Yahoo!気象情報API、LINE Messaging APIのキー取得、FirebaseのBlazeプラン化）
*   **フェーズ2**: アカウント連携の仕組み作り（ユーザーのLINE IDとFirebaseのユーザーデータ（登録住所等）の紐付け）
*   **フェーズ3**: Cloud Functionsの開発（LINEからのWebhook受信、天気APIへのリクエスト処理、返信メッセージの生成）
*   **フェーズ4**: サーバーへの公開とLINEの接続（Firebaseへのデプロイ、LINE側でのWebhook URLの設定）
*   **フェーズ5**: LINEのUI改修とテスト（リッチメニューの更新、実機での動作テスト）

### 🌟 【大型プロジェクト】「お散歩なう」ハイブリッド可視化機能（ストーリーズ風＆マップ表示）
**コンセプト**: お散歩開始時にワンタップで自分のペットアイコンを30分間だけ表示させ、ご近所のワン友とお散歩のタイミングを合わせやすくする機能。プライバシーに配慮しつつ、アプリを開いて1秒で「誰かがお散歩中」だと気づけるUXを目指す。

*   ✅ **マイページの拡張（プロフィールアイコン登録）**: ユーザーが自分のペットの画像（アイコン）をアップロード・登録できる仕組みの構築。
*   ✅ **「お散歩開始」ワンタップアクションの実装**: アクションメニューに専用ボタン（🐶お散歩出発！）を設け、タップすると「現在アクティブ状態（30分間）」としてデータベースに登録される処理。
*   **マップ上への一時的なアイコン（ピン）表示**: アクティブなユーザーの現在地（または出発地点）に、登録されたアイコン画像を丸く切り抜いてマップ上に一時表示させる処理。
*   **30分自動クリアロジック**: アクティブ状態になってから30分経過したユーザーを、ボトムシートおよびマップの表示から自動的に除外（非表示化）するフィルタリング処理。

## 4. ✅ 完了した主要タスク（Done）

*   [x] 自分だけのマップを作れる「マイマップ機能」の実装
    - 各スポット投稿時に「みんなに公開」か「自分だけ（非公開無期限）」かを選択できる設定を追加。
    - マップ上部に「みんなのマップ」と「マイマップ」を切り替えるタブ（セグメントコントロール）を配置。
    - 48時間の制限なく、自分のお気に入り投稿をストックできる「⭐️ マイマップに保存」機能を実装。
*   [x] 「お守りカード（旧：緊急カード）」の画像化・シェア機能の実装
    *   防災・みまもり機能強化のため、マイページのプロフィールを html2canvas で画像化する機能を実装。
    *   外部画像読み込み時のCORSエラー（Taint）を解決。
    *   Web Share API (navigator.share) を利用したネイティブシェア（AirDrop等）に対応。PWA（ホーム画面追加時）での完全動作を確認。
    *   LINE内ブラウザ等でShare APIが弾かれた場合のフォールバックとして、エラーではなく「仕様により端末に画像を直接ダウンロードしました📥」というポジティブなUXでの画像保存処理を実装。
    *   親しみやすさ向上のため、UIの名称を「緊急カード」から「お守りカード」へ変更。
*   [x] 「お散歩開始」ワンタップアクションの実装
*   [x] ピンが重なっている場所での新規投稿がしづらい問題のUI改修（ターゲット照準式とボタン位置の下部配置による操作性向上）
*   [x] 「お知らせ」と「使い方」を兼ねた『ガイド画面』の作成（アップデート履歴の記載欄、LINE VOOMへのリンクボタンの実装）
*   [x] マップの初期表示ズームレベルの調整（広域すぎず、自宅周辺が見やすい16レベルへ変更）
*   [x] ボトムシートUIの実装と、横スクロールタブ化
*   [x] フローティング投稿ボタン（🐾FAB）とキャンセル挙動の実装（モーダル背景タップでのクローズ機能）
*   [x] マップ＆ボトムシートの操作性向上（リスト内カードの日付見切れ解消、ハート💖ボタンの配置最適化など）
*   [x] 投稿から48時間経過したピンの半透明化（過去ログのハイブリッド表示方式へ改修）
*   [x] オリジナル起動画面（スプラッシュスクリーン）の実装（筑波山と犬の画像、最低表示時間1.5秒のフラッシュバック防止制御機能）
*   [x] PWA（スタンドアロン起動機能・ホーム画面追加）対応および専用アイコン（manifest.json）の実装

## 5. 💡 将来のアイデア・実装予定（Backlog）

### 🌟 【将来の構想】「ワン友（相互承認）」システムと高精度表示の解放
*   **概要**: ユーザー同士が「ワン友」として相互承認できるフレンド機能を実装する。
*   **ご褒美（リワード）設計**: 通常のユーザーには引き続き「ゆるふわな範囲（半径3km圏内など）」でのアクティブ表示のみを行うが、ワン友同士になったユーザー間のみ、より精度の高い位置情報（お散歩ルートの共有や、現在の正確なピン表示など）を解放する。
*   **目的**: プライバシー保護の徹底と、アプリを使い込むことへのモチベーション（リテンション）向上を両立させる。

### 🌟 【中長期ロードマップ】ユーザー定着・プレミアム機能の構想

*   **過去アーカイブ閲覧（ログインボーナス）**: 基本は48時間で消えるリアルタイム情報を、Google認証ユーザー限定で「過去7日間・1ヶ月・3ヶ月」のように期間集計データとして振り返ることができる機能。（会員登録の強力な動機付け）
*   ✅ **「ピン留め」＆マイマップ育成機能**: マイマップ機能として実装完了（自作ピンの非公開・無期限保存、および他ユーザーの有益な公開情報の保存機能）。
