name: CI

on:
  push:
    branches:
      - main
      - 'recovery/**'
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install dependencies
        # react-spring-bottom-sheet 3.x supports React through 18 in its peer
        # metadata, while this recovered app already runs on React 19.
        run: npm ci --legacy-peer-deps

      - name: Install Functions dependencies
        run: npm ci --prefix functions

      - name: Install Firebase CLI
        run: npm install --global firebase-tools@14.11.0

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Test Functions aggregation
        run: npm test --prefix functions

      - name: Firebase Emulator permissions
        run: npm run test:emulator:ci
