rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Local emulator only. Production rules remain managed separately.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
